<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="Veil Minimal Quality Gates"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 http://pmd.sourceforge.net/ruleset_2_0_0.xsd">

    <description>Veil quality gates: ONLY complexity (CC > 8), method length (> 40), parameters (> 4), and CPD duplication (100+ tokens)</description>

    <!-- Cyclomatic Complexity: fail if method CC > 8; no class-level aggregate check -->
    <rule ref="category/java/design.xml/CyclomaticComplexity">
        <properties>
            <property name="methodReportLevel" value="8"/>
            <property name="classReportLevel" value="9999"/>
        </properties>
    </rule>

    <!-- Excessive Parameter List: fail if > 4 parameters. Confirmed empirically
         2026-09-06 that this rule's `minimum` property is a strictly-greater-than
         threshold, not a minimum-to-trigger — with minimum=5, a 5-parameter method
         passed and only 6+ failed, which is why this was previously (wrongly) set
         to 5. minimum=4 is what actually makes 5 the first value that fails. -->
    <rule ref="category/java/design.xml/ExcessiveParameterList">
        <properties>
            <property name="minimum" value="4"/>
        </properties>
    </rule>

    <!-- Method Length: fail if > 40 lines (NCSS = non-comment source statements) -->
    <rule ref="category/java/design.xml/NcssCount">
        <properties>
            <property name="methodReportLevel" value="41"/>
        </properties>
    </rule>

</ruleset>
